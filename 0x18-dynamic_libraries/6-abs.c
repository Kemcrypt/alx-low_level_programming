#include "main.h"

/**
 * _abs - prints absolute value of an integer
 *  @n: n is an integer
 *
 *  Description: prints the absolute value of an integer
 *
 *  Return: Always 0.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
